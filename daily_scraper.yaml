name: Daily Lotto Result Auto-Scraper

on:
  schedule:
    # Runs automatically every day at 7:15 PM UTC (8:15 PM WAT)
    - cron: '15 19 * * *'
  workflow_dispatch: # Allows manual trigger button inside GitHub

jobs:
  scrape-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 firebase-admin

      - name: Create Firebase Key File from Secret
        env:
          FIREBASE_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          echo "$FIREBASE_KEY" > firebase_key.json

      - name: Run Auto Scraper
        run: |
          python scraper.py

      - name: Clean up keys
        run: rm firebase_key.json